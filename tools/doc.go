// Package tools provides tool definitions, toolset aggregation, validation,
// and the tool execution loop (ToolLoop, StreamWithToolHandling) for
// multi-round tool calling with the LLM runtime.
package tools
